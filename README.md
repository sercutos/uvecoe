# openECOE-Desktop

 `git clone <tu-repo> `

 `cd <tu-repo> `

 `npm install ` → instala todas las dependencias

 `npm start ` → inicia la app de Electron
## Desarrollo
###  abrir consola:
 `npm run dev `
### abrir otra consola:
 `npm start `
